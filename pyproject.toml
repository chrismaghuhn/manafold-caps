[project]
name = "manafold-cap-miner"
version = "0.1.0"
description = "Small exploratory miner for MTG implementation evidence"
requires-python = ">=3.12"
dependencies = ["datasets>=3.0", "huggingface_hub>=0.26", "PyYAML>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
