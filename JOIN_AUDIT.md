# Phase 0.1.2 — Suspicious Join Audit

Pinned input revisions: Scryfall `0ce026779dae1a9a6448ef7a85e606d662343f5e`, Forge `cef86f363d7f7d5b3293248a75f550a7c3404066`, XMage `6212eb37907c1ce751d8a3fea8b3322056dc0264`.

## Summary

Audited 58 records: Forge 38, XMage 20. Decisions: `{'KEEP_JOIN': 49, 'KEEP_WITH_FLAG': 9, 'REJECT_JOIN': 0}`. Results: `{'VALID_JOIN_STALE_WORDING': 49, 'SOURCE_RECORD_WRONG_OR_CORRUPT': 9}`. Unresolved: 0.

All 58 audit candidates had an exact normalized name that selected one Scryfall identity. Text similarity was diagnostic only. No record was rejected; join review is not validation of requirement semantics.

## Rejected joins

None.

## Flagged joins

- `xmage` `Expedition Skulker` (`1b6e0d65-202e-4fd3-861e-8d2eaffe3269`): The exact normalized name `Expedition Skulker` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Vampire Rogue` and mana cost `{1}{B}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Faerie Seer` (`b2e65e8b-5f08-4cc2-ab1d-00f8903dbea2`): The exact normalized name `Faerie Seer` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Faerie Wizard` and mana cost `{U}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Fanatical Firebrand` (`d36e11f1-6ab3-4273-8114-a8fbbe21c1c3`): The exact normalized name `Fanatical Firebrand` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Goblin Pirate` and mana cost `{R}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Kargan Dragonrider` (`df366871-8ed7-4183-8b0c-414ca361a711`): The exact normalized name `Kargan Dragonrider` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Human Warrior` and mana cost `{1}{R}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Koma's Faithful` (`1d705d77-4b1a-4a17-b3b7-e502f2f6f6e6`): The exact normalized name `Koma's Faithful` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Elf Cleric` and mana cost `{2}{B}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Pelakka Wurm` (`d36075c2-de66-4202-9217-b1102a2bc14b`): The exact normalized name `Pelakka Wurm` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Wurm` and mana cost `{4}{G}{G}{G}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Undersea Invader` (`ff12dfee-7140-44bd-a62e-5f8ab98e93c6`): The exact normalized name `Undersea Invader` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Giant Rogue` and mana cost `{4}{U}{U}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Wailing Ghoul` (`6ce24851-70d2-416c-8733-78928b4eee74`): The exact normalized name `Wailing Ghoul` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Zombie` and mana cost `{1}{B}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.
- `xmage` `Wall of Lost Thoughts` (`49b80368-d38a-4877-b1de-f3b5f3d40d4d`): The exact normalized name `Wall of Lost Thoughts` has one candidate in the pinned Scryfall name index. XMage prompt has type line `Creature â€” Wall` and mana cost `{1}{U}` matching Scryfall. The XMage type line contains the mojibake sequence U+00E2 U+20AC U+201D (UTF-8 em dash decoded as Windows-1252/Latin-1); its type words normalize to the Scryfall type line, and the prompt mana cost and rules text agree. Keep the identity join but flag the corrupt type-line field. The text variant is also historical: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text.

## Valid stale/variant joins

49 records were retained as `VALID_JOIN_STALE_WORDING`. The source records use historical Oracle wording such as printed card names for self-reference, “enters the battlefield,” or reminder-text variants. Exact unique names and the available type/mana context agree with the pinned Scryfall identity. Representative records:

- `forge` `Crooked Custodian` — The exact normalized name `Crooked Custodian` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Ogre Rogue` and mana cost `{1}{B}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses old “enters the battlefield” wording instead of the current “enters” template; uses the printed card name for self-reference where current Oracle uses “this …”. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Crystal Grotto` — The exact normalized name `Crystal Grotto` has one candidate in the pinned Scryfall name index. Forge input has type line `Land` and mana cost ``, both matching the Scryfall row. The text difference is historical Oracle wording: uses old “enters the battlefield” wording instead of the current “enters” template; uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Daggersail Aeronaut` — The exact normalized name `Daggersail Aeronaut` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Goblin` and mana cost `{3}{R}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses the printed card name for self-reference where current Oracle uses “this …”; uses the older “as long as it’s your turn” condition template. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Diregraf Ghoul` — The exact normalized name `Diregraf Ghoul` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Zombie` and mana cost `{B}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses old “enters the battlefield” wording instead of the current “enters” template; uses the printed card name for self-reference where current Oracle uses “this …”. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Elturel Survivors` — The exact normalized name `Elturel Survivors` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Tiefling Peasant` and mana cost `{3}{R}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Expedition Skulker` — The exact normalized name `Expedition Skulker` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Vampire Rogue` and mana cost `{1}{B}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Faerie Seer` — The exact normalized name `Faerie Seer` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Faerie Wizard` and mana cost `{U}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses old “enters the battlefield” wording instead of the current “enters” template; uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.
- `forge` `Fanatical Firebrand` — The exact normalized name `Fanatical Firebrand` has one candidate in the pinned Scryfall name index. Forge input has type line `Creature — Goblin Pirate` and mana cost `{R}`, both matching the Scryfall row. The text difference is historical Oracle wording: uses the printed card name for self-reference where current Oracle uses “this …”; reminder text is omitted or worded differently between the source and current Oracle text. The source and current text retain the same card-specific ability/effect clauses and show no competing-card or face signal. Keep the identity join and preserve both text versions unchanged.

## Systematic issues found

No normalized-name collision or multiface collision occurred among the 58. All were `normalized_name` joins; the name index resolved each to one Scryfall row, and no Oracle ID was available in the external records. All source type lines match Scryfall except nine XMage prompts whose em dash is encoded as the mojibake sequence U+00E2 U+20AC U+201D. Those nine have matching type words after that known encoding normalization and retain matching names/mana context; they are `SOURCE_RECORD_WRONG_OR_CORRUPT` with `KEEP_WITH_FLAG`. No bug was found in card-name extraction, Oracle-text extraction, face indexing, ambiguity detection, or Oracle-ID precedence.

## Matching changes

Before / after distinct identities — Forge: 26384 / 26384; XMage: 18252 / 18252; both: 17270 / 17270.
Mapped candidates — total 21251 / 21251; multi-implementation 5828 / 5828. No rejected joins means downstream counts are unchanged. `KEEP_WITH_FLAG` metadata is attached to the affected matched evidence and requirement evidence.

## Recommendation for Phase 0.2

READY. The exact-name joins in this narrowly audited set are supported by unique identity and card context; none should be excluded. Carry the nine XMage encoding warnings forward. This decision is about identity only and does not assert that source Oracle wording or extracted implementation semantics are correct.
